Flask==3.1.3
langchain-core==1.4.9
langchain-openai==1.3.5
langgraph==1.2.9
# The Rust builds `uuid-utils` and `tiktoken`/`tiktoken_ext` (transitive deps of
# langchain-core / langchain-openai) link `ndk-context` and panic on Termux
# ("android context not initialized"), aborting the server when LangGraph calls
# uuid4 during /v1/rag/search. Do NOT install them — the pure-Python `./uuid_utils`
# and `./tiktoken` shims in this directory are used instead.
#   pip uninstall -y uuid-utils tiktoken tiktoken_ext

# RAG (web search + retrieval). No browser is required: DuckDuckGo search uses
# `ddgs`, pages are crawled with the stdlib urllib + html.parser, and BM25 is
# implemented in-repo (`rag.index.BM25`). `pypdf`/`python-docx` handle local
# document uploads.
ddgs
pypdf
python-docx
